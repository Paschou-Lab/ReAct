CompileDir=$1

gcc -c cdflib.c
ar -rc libcdf.a *.o
ranlib libcdf.a

gcc -c toms462.c
ar rcs libBiNormP.a toms462.o

gcc Main.c CountConstruct.c Preparation.c PvalCompute.c SupportFunc.c -L. -lcdf -lBiNormP -o ${CompileDir}/MetaAnalysis -lm -lgsl -lgslcblas
